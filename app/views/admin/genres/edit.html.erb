<br>
 <main>
   <div class="container", align="center">
    <div class="row">
      <div class="col-sm-12 col-md-8 col-lg-4 px-5 px-sm-10">

      	 <% if @genre.errors.any? %>
      　 <%= @genre.errors.count %>error prohibited this obj from being saved:
         <% @genre.errors.full_messages.each do |message| %>
         <%= message %>
         <% end %>
         <% end %>

        <table style="border:none;" width="500">
         <thread>
           <tr>
             <th><h5>ジャンル編集</h5></th><br>

             <%= form_with model:@genre, local:true do |f| %>
             <div class="form-group">
               <th><%= f.text_field :name %></th>
             </div>

             <th><%= f.submit '編集', class: "btn btn-secondary" %></th>
            <% end %>

           </tr>
         </thread>
        </table>
      </div>
    </div>
   </div>
  </main>